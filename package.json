{
  "name": "next-talk",
  "private": "true",
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "dev:web": "lerna run dev:web --stream --parallel",
    "build": "lerna run build"
  },
  "devDependencies": {
    "@commitlint/cli": "^11.0.0",
    "@commitlint/config-conventional": "^11.0.0",
    "husky": "^4.3.8",
    "lerna": "^3.22.1"
  }
}
