{
  "name": "inboxed",
  "private": "true",
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "postbootstrap": "npm run build",
    "dev:web": "lerna run dev --stream --parallel  --scope '{@inboxed/api,@inboxed/web}'",
    "dev:mobile": "lerna run dev --stream --parallel --scope '{@inboxed/api,@inboxed/mobile}'",
    "build": "lerna run build",
    "lint": "lerna run lint",
    "test": "lerna run test"
  },
  "devDependencies": {
    "@commitlint/cli": "^11.0.0",
    "@commitlint/config-conventional": "^11.0.0",
    "husky": "^4.3.8",
    "lerna": "^3.22.1"
  }
}
